
export type Course_class_listProps = {
    stage: number,
    semesterCode: number,
    subjectCode: string,
}
const Course_class_list = ({stage, semesterCode, subjectCode}: Course_class_listProps) => {
    return (
        <>
            
        </>
    )
}