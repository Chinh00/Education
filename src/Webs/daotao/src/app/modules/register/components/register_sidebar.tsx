import { memo } from "react";

const RegisterSidebar = () => {
    return (
        <div className={"p-4 border-t-2 border-r-2 h-screen"}>
            RegisterSidebar
        </div>
    )
}
export default memo(RegisterSidebar);