export interface ClassManager {
    classCode: string;
    className: string;
    educationCode: string;
    semesterClasses: any[]; 
    id: string;
    createdAt: string; 
    updatedAt: string | null;
}

