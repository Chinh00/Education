export interface Room {
    capacity: number;
    code: string;
    name: string;
    examCapacity: number;
    buildingCode: string;
    id: string;
    createdAt: string;
    updatedAt: string | null;
}