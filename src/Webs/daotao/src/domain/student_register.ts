export interface StudentRegister {
    studentCode: string;
    correlationId: string;
    subjectCodes: string[];
    educationCode: string;
    registerDate: string;
    id: string;
    createdAt: string;
    updatedAt: string | null;
}